run npm init
name cant have caps init so name as "res-api"
version is 0.0.1
change our main to "dist"
npm install --save express mongoose body-parser
// Installing dev helpers
npm install --save-dev nodemon eslint babel-cli babel-core babel-eslint babel-preset-env babel-preset-stage-0
add object "eslintConfig" in package json
for babel config, create a new file in main project file named .babelrc
after adding babel resets, we add something in our scripts, one for development
nodemon -w src --exec - w src to watch source folder for changes and 
mkdir src
add file index.js to src folder
npm run build should copy file index.js to dist folder
npm run lint should show us express was imported but never used

// Setting up mongo db for the API
import http, body-parser, express, mongoose
import the files we are going to create

anything coming to /v1 , we will send to routes
export default app only works with es6

create config folder and create an index.js
create routes folder and create an index.js

// authentication
npm install --save express-jwt jsonwebtoken passport passport-local passport-local-mongoose
